# Terraform AWS RDS (Scaffold)

Minimal Terraform setup to stand up an RDS Postgres instance. The provider config relies on default tags, so all resources are labeled consistently.

So far this is just the most basic config/staring point. 